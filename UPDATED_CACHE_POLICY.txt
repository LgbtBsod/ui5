UPDATED CACHE POLICY

Detail cache
- Current detail open snapshot excludes attachments.
- Current detail open snapshot is phased: root/basic at open, checks/barriers by deferred second-phase read.
- Persisted save writes base snapshot without attachment binary payload, but attachment lists are still synchronized separately into detail/view state.

Freshness validation
- LastChangeSet remains the aggregate freshness checkpoint.
- Cache validation is used before sensitive edit/save flows.

Attachment note
- Attachment binaries are not embedded into the base detail cache snapshot.
- Persisted attachment state stays in the detail model and staged session uploads stay in the view model.

Runtime settings
- Runtime settings are cached with TTL in SettingsManager.
- Canonical normalized runtime values are written to masterData>/runtime/*.

Search request window
- Backend fetch limit and visible page size remain separate.

Truth status
- Attachment binary exclusion from cache: matched.
- Full phased detail cache/open policy: matched for initial open and deferred child hydration; attachment state separation remains partial.

UPDATED LOCK STATE MACHINE

Observed states
- READ_ONLY
- ACQUIRING_LOCK
- EDIT_LOCKED
- LOCK_LOST
- FORCED_READ_ONLY
- IDLE_TIMEOUT_GRACE

Observed operations
- acquire
- heartbeat
- status
- release
- takeover via ForceTakeover on acquire

Canonical Gateway payloads
- LockAcquire(DB_KEY, SessionGuid, TabSessionId, ForceTakeover)
- LockHeartbeat(DB_KEY, SessionGuid)
- LockRelease(DB_KEY, SessionGuid)

Truth notes
- Current lock contract is aligned at the frontend and metadata boundary on DB_KEY.
- Backend DPC still tolerates ObjectUuid as a narrow compatibility ingress fallback while resolving DB_KEY first.
- Page-leave release remains best-effort only and session-aware.
- lock acquire before integration-origin confirm is not the intended flow, but full all-entry-path proof remains outside current frontend-only evidence.

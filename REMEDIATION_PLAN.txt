TZ 2.1 remediation executed against current repository truth.

Closed in this pass:
- active detail lifecycle switched to DRAFT / IN_PROCESS / COMPLETED / CANCELLED
- direct checklist DELETE removed from target frontend flow; delete now goes through SaveChanges aggregate command with root.edit_mode = D
- direct attachment DELETE removed from target frontend flow; persisted attachment removal is deferred into aggregate delta/save flow
- initial detail open no longer loads attachments into snapshot or detail cache payload
- copy command aligned to SourceUuid to match archive command contract
- local metadata trimmed for lock command drift and made metadata-first for attachments

Still documented instead of invented:
- full backend ArchiveLink media-stream replacement for Value fallback
- end-to-end integration edit confirmation dispatch before permission/lock in every entry path
- final backend analytics archive topology beyond current repo evidence

# Backend Unknowns

- Full productive proof that every active command-flow path uses `DB_KEY / PARENT_KEY` as canonical technical identity is not yet established only from this scan.
- Productive chunk transport contract for checks/barriers loading is not fully proven from the scanned frontend and available backend snippets.
- Productive ArchiveLink media/content provider implementation is outside this repo.
- Some lock status semantics are inferred from adapter normalization and local metadata, but full productive Gateway entity implementation for `LockStatusSet` is not fully evidenced in scanned backend code.
- Official SAPUI5 registry-backed build behavior for 1.71.70 depends on external SAP registry access and cannot be proven inside this repo alone.

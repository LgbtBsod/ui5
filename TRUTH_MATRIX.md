# Truth Matrix

| Requirement | Expected by TZ | Actual in code | Status | Files involved | Fix strategy |
| --- | --- | --- | --- | --- | --- |
| Canonical technical keys | `DB_KEY` / `PARENT_KEY` are canonical | Active read/save/snapshot/delta paths no longer scatter repeated alias lists; alias knowledge is centralized in `ODataEntityContracts.IDENTITY`. Full backend proof that `DB_KEY/PARENT_KEY` are the only productive canonical keys still cannot be derived from this repo alone | Partial | `app/infra/adapters/shared/ODataEntityContracts.js`, `app/infra/adapters/shared/ODataChecklistPayloadMapper.js`, `app/service/shared/delta/DeltaFieldMappers.js`, `app/service/shared/DeltaPayloadBuilder.js`, `app/infra/adapters/shared/ChecklistSnapshotMapper.js` | Keep alias handling at one boundary owner, then verify remaining backend identity assumptions separately |
| Detail initial load | Load root/basic first, heavy children after, no attachments on open | Attachments already excluded from open snapshot; phased root/basic truth partially documented, chunked child loading not yet fully proven from current scan | Partial | `app/service/domain/detail/usecases/OpenDetailUseCase.js`, `app/infra/adapters/shared/ODataChecklistReadRuntime.js` | Verify child loading path and document/prove chunk owner |
| Attachments metadata-first | Lazy, metadata-first, `Value` not source of truth | Active open flow now prefers `DownloadUrl`; `Value` remains compatibility fallback; metadata-first is in place | Match | `app/service/features/detail/runtime/DetailAttachmentOpenRuntime.js`, `app/localService/metadata.xml`, `app/infra/adapters/shared/ODataEntityContracts.js` | Keep fallback documented and avoid spreading `Value` dependency |
| Delete pipeline | Persisted delete via modification pipeline, not direct DELETE | Checklist/attachment delete target flow already aligned to save pipeline | Match | `app/service/domain/detail/usecases/AttachmentDeleteUseCase.js`, `app/infra/adapters/shared/ODataChecklistStatusRuntime.js`, `app/service/backend/GatewayClient.js` | Keep direct DELETE unsupported |
| Lock flow | acquire/status/heartbeat/release, self-steal only by `ForceTakeover` | Lock adapter aligned; LockStatus metadata enriched; neutral `releaseActiveLock` added; deprecated alias remains | Match | `app/infra/adapters/LockAdapter.js`, `app/service/framework/behavior/WorkflowDecisionRuntime.js`, `app/localService/metadata.xml` | Keep alias transitional, do not reintroduce parallel takeover semantics |
| Integration confirm-before-edit | Confirm first, only then permission/cache/lock/edit | Confirm gate now added in enter-edit flow via existing usecase/effect path | Match | `app/service/domain/detail/usecases/EnterEditUseCase.js`, `app/service/domain/detail/DetailWorkflowRuntime.js`, `app/service/runtime/component/ComponentFacadeEffectRuntime.js` | Keep single owner in usecase/effect flow |
| Runtime settings source | `masterData>/runtime/*` is canonical source | Current code already loads from `RuntimeSettingsSet('GLOBAL')` and projects to `masterData>/runtime/*`; state acts as projection | Match | `app/service/runtime/SettingsManager.js`, `app/service/domain/shared/usecases/ApplyRuntimeSettingsUseCase.js` | Remove ambiguous comments/raw endpoint usage only |
| Search semantics | Exact=AND, Loose=OR, date_check desc default, no invented logic | Current code matches this; still fragmented across multiple controller/runtime files | Match | `app/service/features/search/contracts/SearchFilterBuilder.js`, `app/service/features/search/contracts/SearchBindingPolicy.js`, `app/controller/search/*` | Preserve semantics while reducing wrappers |
| Constants extraction | Repeated technical strings live in constants modules | Good baseline exists, but repeated technical strings still remain across controllers/services/styles | Partial | `app/constants/*`, `app/controller/*`, `app/service/*`, `app/styles/modules/*` | Build extraction plan and move only repeated technical values |
| One owner per behavior | No duplicate owners for same flow | Detail/search/app/analytics still have duplicated behavior ownership across controller behavior/runtime/helper files | Mismatch | `app/controller/detail/*`, `app/controller/search/*`, `app/controller/app/*`, `app/controller/analytics/*` | Merge thin wrappers into canonical owners incrementally |
| CSS/DOM SAP best practices | Avoid internal SAP CSS patching and DOM hacks | High CSS debt on `.sap*` internals and DOM access patterns | Mismatch | `app/styles/modules/*.css`, `app/controller/detail/*`, `app/service/features/search/runtime/*` | Inventory violations, replace with app-owned classes or document residual |

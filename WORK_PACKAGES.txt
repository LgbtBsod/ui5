WORK PACKAGES

WP1. Artifact truth correction
- rewrite optimistic matched/closed wording
- create missing txt owner/constants/merge artifacts

WP2. Key contract correction
- reduce alias fallback chains
- restore RootKey/PARENT_KEY as primary command-flow identifiers
- keep pcct_uuid/ObjectUuid only as boundary compatibility fields

WP3. Detail phased loading
- closed in code: root/basic on open, deferred checks/barriers chunk loading, attachments still explicit only
- keep one follow-up only if needed: targeted telemetry or tests for deferred child-load failure path

WP4. Attachment contract correction
- partially closed in code: persisted vs session attachment state separated, dual-section rendering added
- remaining work: isolate Value to compatibility fallback only and remove Value-centric normalization/open tails

WP5. Remaining overengineering reduction
- component bootstrap cluster
- component guard/lock clusters
- app shell cluster
- constants simplification

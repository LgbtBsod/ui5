TRUTH MATRIX

1. Key contract / canonical technical identity
- Current state:
  - `activeObjectId` and `selectedId` are the working frontend/runtime keys.
  - `RootId` is compatibility-only at the boundary and should not be promoted as a productive model key.
- Status:
  - Mostly aligned.

2. Detail initial loading
- Current state:
  - root/basic open stays separate from later checks/barriers hydration.
  - attachments remain explicit and non-default.
- Status:
  - Match.

3. Attachments metadata-first contract
- Current state:
  - productive open/download uses `DownloadUrl` / `DocumentHandle`.
  - `Value` is no longer part of default attachment normalization/selects.
- Status:
  - Match.

4. Delete pipeline
- Current state:
  - aggregate mutation flow remains canonical.
  - no direct target delete flow was reintroduced.
- Status:
  - Match.

5. Lock flow
- Current state:
  - ForceTakeover, lock status, heartbeat, and release remain intact.
  - `ObjectUuid` / `RootKey` mappings stay boundary-local.
- Status:
  - Match.

6. Overengineering / hidden framework
- Current state:
  - thin wrappers were removed from component bootstrap/listener/navigation guard paths.
  - some controller and detail owners still remain as real behavior boundaries.
- Status:
  - Improved, but not fully flat.

7. SAP CSS / DOM best practices
- Current state:
  - private selector usage is reduced, not eliminated.
  - DOM hacks remain only where UI5 public APIs are insufficient.
- Status:
  - Residual risk remains.

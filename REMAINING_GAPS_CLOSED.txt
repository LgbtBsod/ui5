REMAINING GAPS STATUS

Actually closed in code
- Direct OData DELETE is not the target frontend delete flow.
- Lock acquire/heartbeat/release contract is aligned on ObjectUuid + SessionGuid, with ForceTakeover only on acquire.
- Search exact/loose semantics and backend fetch limit vs visible chunk size are preserved.
- Runtime settings still project to masterData>/runtime/*.

Not actually closed yet
- Key contract is not fully closed: command payload still centers on pcct_uuid and broad alias fallback chains.
- Attachment contract is not fully closed: Value remains part of active save/open/select normalization.
- Attachment dual-table separation is not closed: persisted and session attachments still collapse into view>/sessionAttachments.

Compatibility tails
- ObjectUuid remains a justified Gateway boundary field for lock function imports.
- Value remains a compatibility fallback, but is still too active to call the attachment contract fully metadata-first.

Closed in this pass
- Detail phased loading is now closed in frontend code: initial open loads root/basic only, checks/barriers hydrate in a deferred second phase with chunk size 20, attachments remain explicit only.

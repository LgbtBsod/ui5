<mvc:View
    controllerName="checklist.app.controller.Analytics"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">

    <Page
        showHeader="false"
        enableScrolling="true"
        class="pageTransparent"
        busy="{view>/busy}"
        busyIndicatorDelay="0">
        <content>
            <VBox class="appContent analyticsScreenContent" renderType="Bare">
                <VBox class="glassCard analyticsHeroCard" renderType="Bare">
                    <VBox class="analyticsHeroCopy" renderType="Bare">
                        <Text text="{i18n>analyticsScreenEyebrow}" class="workflowEyebrow"/>
                        <Title text="{i18n>workflowAnalyticsTitle}" level="H2" class="analyticsScreenTitle"/>
                        <Text text="{i18n>analyticsScreenSubtitle}" class="analyticsScreenSubtitle"/>
                    </VBox>

                    <HBox class="analyticsHeroActions" renderType="Bare" wrap="Wrap">
                        <Button
                            text="{i18n>retryButton}"
                            icon="sap-icon://refresh"
                            type="Transparent"
                            press=".onRefreshAnalytics"
                            class="brandActionBtn brandActionGhost"/>
                        <Button
                            text="{i18n>closeButton}"
                            icon="sap-icon://nav-back"
                            type="Emphasized"
                            press=".onCloseAnalytics"
                            class="brandActionBtn brandActionPrimary"/>
                    </HBox>
                </VBox>

                <VBox class="analyticsScreenBody" renderType="Bare">
                    <MessageStrip
                        text="{i18n>workflowAnalyticsBackendNote}"
                        type="Information"
                        showIcon="true"
                        class="chkFeedbackStrip"/>
                    <MessageStrip
                        text="{view>/error}"
                        type="Warning"
                        showIcon="true"
                        visible="{= !!${view>/error} }"
                        class="chkFeedbackStrip chkFeedbackWarning"/>
                    <HBox class="analyticsHeroActions sapUiSmallMarginBottom" renderType="Bare" wrap="Wrap" alignItems="Center">
                        <Label text="{i18n>analyticsYearLabel}" labelFor="analyticsYearSelect" class="sapUiTinyMarginEnd"/>
                        <Select
                            id="analyticsYearSelect"
                            width="10rem"
                            selectedKey="{view>/selectedYear}"
                            change=".onSelectAnalyticsYear"
                            items="{view>/availableYears}">
                            <core:Item key="{view>key}" text="{view>text}"/>
                        </Select>
                    </HBox>
                    <core:Fragment fragmentName="checklist.app.view.fragment.WorkflowAnalyticsTopline" type="XML"/>
                    <core:Fragment fragmentName="checklist.app.view.fragment.WorkflowAnalyticsBreakdowns" type="XML"/>
                </VBox>
            </VBox>
        </content>
    </Page>
</mvc:View>

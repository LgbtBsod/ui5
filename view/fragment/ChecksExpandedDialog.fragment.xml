<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
    <Dialog class="glassDialog" id="checksExpandedDialog" title="{i18n>checksExpandedTitle}" contentWidth="72rem" contentHeight="36rem" draggable="true" resizable="true" stretchOnPhone="true">
        <content>
            <VBox class="expandedDialogContent" renderType="Bare">
                <Table items="{selected>/checks}" class="flatEditorTable" growing="true" growingThreshold="20">
                    <columns>
                        <Column width="3rem"><Text text=""/></Column>
                        <Column><Text text="{i18n>checkLabel}"/></Column>
                        <Column><Text text="{i18n>statusLabel}"/></Column>
                        <Column><Text text="{i18n>commentLabel}"/></Column>
                        <Column width="2.5rem" visible="{= ${state>/mode} === 'EDIT' }"><Text text=""/></Column>
                    </columns>
                    <items>
                        <ColumnListItem>
                            <cells>
                                <CheckBox selected="{selected>selected}" select=".onSelectionToggle" visible="{= ${state>/mode} === 'EDIT' }"/>
                                <Input value="{selected>text}" editable="{= ${state>/mode} === 'EDIT' }"/>
                                <Switch state="{selected>result}" enabled="{= ${state>/mode} === 'EDIT' }"/>
                                <Input value="{selected>comment}" editable="{= ${state>/mode} === 'EDIT' }" placeholder="{i18n>commentPlaceholder}"/>
                                <Button icon="sap-icon://delete" type="Transparent" press=".onDeleteCheckRow" visible="{= ${state>/mode} === 'EDIT' }"/>
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </VBox>
        </content>
        <beginButton>
            <Button text="{i18n>closeButton}" press=".onCloseChecksExpanded"/>
        </beginButton>
    </Dialog>
</core:FragmentDefinition>

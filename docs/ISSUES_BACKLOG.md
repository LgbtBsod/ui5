# Issues Backlog

Last updated: 2026-03-04

| ID | Severity | Steps | Expected | Actual | Root cause | Fix | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| UX-001 | High | Open detail on a narrow viewport and expand Checks | Mobile table should replace desktop grid | Desktop grid remained visible; mobile fallback did not appear | Visibility depended on `device>/system/phone` instead of viewport-aware state | Bound detail tables/dialogs to `appView>/isPhoneViewport` and synced viewport state in app shell controller | Closed |
| UX-002 | High | Load app home route | Product shell should expose brand, context, notifications, settings, help, and user entry | Only a floating theme toggle existed globally | Shell pattern missing from app root | Added shared `AppShellHeader` plus shell popovers for notifications, help, settings, and user actions | Closed |
| UX-003 | High | Trigger effect-driven warnings, confirmations, or toasts | Feedback should match glass dialog system and button order | Raw `MessageBox` and `MessageToast` appeared outside product styling | Effect pipeline bypassed product-owned feedback wrappers | Routed effect feedback through `glassDialog` and `glassToast` classes | Closed |
| UX-004 | Medium | Use search filters in Morning theme | Inputs and actions should feel actionable and readable | Controls were pale and low-contrast against bright surfaces | Surface contrast was stronger than control contrast | Introduced shared `--theme-workbench-*` rail/control tokens and applied them to filter, action, and table-control surfaces | Closed |
| UX-005 | Medium | Open create draft and inspect attachments area | Upload affordances and helper text should agree | UI warned to save first but upload actions were still exposed | Create sentinel ID was treated as truthy persisted ID | Disabled attachment category and upload actions until a persisted root exists | Closed |
| UX-006 | Medium | Force startup or data-load error | Error text should be readable and localized | Fallback error strings included mojibake | Hardcoded text encoding issue in runtime code | Replaced startup load error fallback with i18n-backed copy | Closed |
| UX-007 | Medium | Run browser smoke pack | Automation should reflect user workflows reliably | Shell-aware layout assertions drifted from the new shell geometry | Smoke script expected viewport-top split and sticky offsets from the pre-shell layout | Updated smoke assertions to validate actual pane alignment and shell-aware sticky clearance | Closed |
| UX-008 | Medium | Compare Morning and Night across same flow | Themes should differ in density and emphasis as well as color | Differences were mostly chromatic and surface-depth based | Theme philosophy was not fully tokenized | Added per-theme shell, workbench, row-height, stat-card, and rail tokens consumed across shell/search/detail | Closed |
| UX-009 | Low | Navigate icon-only controls by keyboard and screen reader | Controls should remain understandable without hover | Row-level actions in checks, barriers, attachments, and expanded dialogs inherited compact icon-only affordances | Row/action affordances still inherited raw UI5 table defaults | Replaced row icon buttons with shared labeled row-action buttons, added explicit action column semantics, and aligned tooltips/a11y naming | Closed |
| UX-010 | Medium | Delete a checklist from Search | Destructive checklist action should appear where full checklist context exists | Delete lived in the search discovery toolbar without detail context | Delete flow was inherited from selection-toolbar logic instead of detail workflow ownership | Moved checklist delete to the detail edit control rail and removed it from Search | Closed |
| UX-011 | High | Open Search and try to reach lower content on shorter viewports | Search page should scroll vertically | Search route was trapped at the top with no vertical page scroll | `sap.m.Page` scrolling was disabled inside the splitter shell | Re-enabled Search page scrolling so the page container owns vertical overflow again | Closed |
